$(function(){
    $('.list').slick({
        // 자동실행
    autoplay: true,
    // 한화면에 보일 슬라이드 갯수 설정
    slidesToShow:2,
    slidesToScroll:1,
    // 좌우버튼 숨기기
    arrows: false,
    // 페이지 버튼 보이기
    dots: true,
  });
})